# Main model (Anthropic Claude)
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# Backup model (MiniMax OpenAI compatible)
MINIMAX_API_KEY=your-minimax-api-key
MINIMAX_BASE_URL=https://api.minimax.chat/v1

# Default model selection
DEFAULT_LLM_PROVIDER=anthropic
# Options: anthropic, openai (compatible with MiniMax)

# Database
DATABASE_URL=sqlite:///./focuspal.db

# Server
HOST=127.0.0.1
PORT=8000
